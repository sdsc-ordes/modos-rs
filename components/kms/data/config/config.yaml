server:
  hostname: localhost
  port: 3000

storage:
  connection:
    endpoint: http://localhost:9010
    usePathStyle: true

oidc:
  issuer: http://localhost:9001/application/o/modos-cli-app
  # issuer: http://localhost:8081/realms/modos
  clientID: modos-cli

log:
  forceDevLog: true
