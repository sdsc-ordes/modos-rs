server:
  hostname: localhost
  port: 3000

storage:
  connection:
    endpoint: http://localhost:9010
    usePathStyle: true

oidc:
  issuer: http://localhost:8081
  clientID: modos-cli

log:
  forceDevLog: true
